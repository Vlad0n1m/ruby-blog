<h1 class="font-bold text-[32px]">Articles</h1>

  <ul class="w-full">
    <% @articles.each do |article| %>

      <li>
      <%= link_to(article) do %>
      <div class="underline mb-4"><%= article.title %></div>
      <% end %>
      </li>
    <% end %>
  </ul>
