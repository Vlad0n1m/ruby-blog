<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="flex flex-col justify-center items-center pt-4">
      <div class="max-w-[700px] w-full flex flex-col justify-center items-center gap-5">
        <div class="flex gap-3 w-full justify-between">
          <%= link_to(about_path) do %>
            <div class="bg-black text-white rounded-lg p-3">About</div>
          <% end %>
          <%= link_to(root_path) do %>
            <div class="bg-black text-white rounded-lg p-3">Home</div>
          <% end %>
          <%= link_to(articles_path) do %>
            <div class="bg-black text-white rounded-lg p-3">Articles</div>
          <% end %>
          <%= link_to(new_article_path) do %>
            <div class="bg-black text-white rounded-lg p-3">New Article</div>
          <% end %>
        </div>
        <main class="w-full flex flex-col gap-5 items-center justify-center">
          <%= yield %>
        </main>

      </div></div>
  </body>
</html>
